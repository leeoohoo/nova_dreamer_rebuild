{
  "servers": [
    {
      "name": "project_files",
      "url": "cmd://node ./mcp_servers/filesystem-server.js --root . --mode read --name project_files",
      "description": "Browse/Search project files (Read-only, defaults to root=.)."
    },
    {
      "name": "code_maintainer",
      "url": "cmd://node ./mcp_servers/code-maintainer-server.js --root . --write --name code_maintainer",
      "description": "Read/write codebase with extra helpers: read_file_raw/read_file_range/stat_path/move_path/copy_path."
    },
    {
      "name": "lsp_bridge",
      "url": "cmd://node ./mcp_servers/lsp-bridge-server.js --root . --name lsp_bridge",
      "description": "LSP bridge (hover/definition/completion/diagnostics via local language servers; requires servers installed)."
    },
    {
      "name": "desktop_commander",
      "url": "cmd://npx -y @wonderwhy-er/desktop-commander",
      "description": "Desktop automation tools (via @wonderwhy-er/desktop-commander)"
    },
    {
      "name": "task_manager",
      "url": "cmd://node ./mcp_servers/task-server.js --root . --store .deepseek_cli/tasks.json --name task_manager",
      "description": "Task board for this workspace: add/list/update/complete/clear tasks."
    },
    {
      "name": "subagent_router",
      "url": "cmd://node ./mcp_servers/subagent-server.js --name subagent_router",
      "description": "Sub-agent directory/router: list, inspect, and run sub-agents directly."
    }
  ]
}

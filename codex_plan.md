# Analysis (pre-task)
Goal: bump the version of the devkit package and publish it to npm. The relevant package is chatos-uiapps-devkit (name: @leeoohoo/ui-apps-devkit). We'll update its package.json version and then run npm publish from that directory.

# Tasks
1) Bump @leeoohoo/ui-apps-devkit version in chatos-uiapps-devkit/package.json.
2) Publish the package to npm from chatos-uiapps-devkit.

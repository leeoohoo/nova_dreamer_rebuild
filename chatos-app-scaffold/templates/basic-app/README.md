# Basic App Template

Starter template for a UI-only ChatOS app. Use this when you do not need MCP tools.

Contents
- UI entrypoints (`index.mjs`, `compact.mjs`)
- Backend optional methods
- No MCP server by default
